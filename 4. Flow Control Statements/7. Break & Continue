#include<stdio.h>

main()
{
    
    /*
    Break and Continue are the key words that are used inside loop statements.
    When the break statement is encountered, the loop immediately terminates and the control goes to the statements following the loop.
    When the continue statement is encountered, the control goes back to the top of the loop ignoring the statements left below it.
    */
    
    int i = 0;
    while(1)
    {
      i++;
      printf("%d", i);
      if()
    }
}
